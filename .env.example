# PROJECT-SWARM Environment Configuration
# Copy this file to .env and fill in your values
# Never commit .env to version control!

# ============================================
# Server Configuration
# ============================================

# Server port (default: 5000)
PORT=5000

# Node environment (development, production, test)
NODE_ENV=development

# ============================================
# Database Configuration
# ============================================

# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# Examples:
#   - Neon: postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/dbname
#   - Local: postgresql://localhost:5432/project_swarm
#   - Supabase: postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
DATABASE_URL=postgresql://localhost:5432/project_swarm

# ============================================
# AI Provider API Keys
# ============================================

# OpenAI API Key
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Anthropic API Key (Claude)
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Google Gemini API Key
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=...

# ============================================
# Authentication & Security
# ============================================

# Session secret for cookie signing (generate with: openssl rand -hex 32)
SESSION_SECRET=your-super-secret-session-key-here

# Encryption key for API key storage (generate with: openssl rand -hex 32)
ENCRYPTION_KEY=your-encryption-key-here

# Encryption salt (generate with: openssl rand -hex 16)
ENCRYPTION_SALT=your-encryption-salt-here

# ============================================
# GitHub OAuth Configuration (Optional)
# ============================================

# GitHub OAuth Client ID
# Get from: https://github.com/settings/developers
GITHUB_CLIENT_ID=your-github-client-id

# GitHub OAuth Client Secret
GITHUB_CLIENT_SECRET=your-github-client-secret

# GitHub OAuth Redirect URI
# Example: http://localhost:5000/api/auth/github/callback
GITHUB_REDIRECT_URI=http://localhost:5000/api/auth/github/callback

# ============================================
# Replit Configuration (Replit Deployments Only)
# ============================================

# Replit Deployment ID
REPL_ID=your-repl-id

# Replit Identity for authentication
REPL_IDENTITY=...

# Replit domain configuration
REPLIT_DOMAINS=your-repl-name.your-username.repl.co

# Replit connectors hostname
REPLIT_CONNECTORS_HOSTNAME=...

# Replit issuer URL
ISSUER_URL=https://replit.com

# Web repl renewal token
WEB_REPL_RENEWAL=...

# ============================================
# Build & Deployment Configuration
# ============================================

# Set to 'true' when building for GitHub Pages
# This configures base path for routing
GITHUB_PAGES=false

# Vite API URL for GitHub Pages deployment
# Example: https://api.example.com
VITE_API_URL=http://localhost:5000

# ============================================
# Optional: Redis Configuration
# ============================================

# Redis connection string for caching (optional, future enhancement)
# Format: redis://user:password@host:port/database
# REDIS_URL=redis://localhost:6379

# ============================================
# Optional: Monitoring & Logging
# ============================================

# Log level (error, warn, info, debug)
# LOG_LEVEL=info

# Sentry DSN for error tracking (optional)
# SENTRY_DSN=https://...@sentry.io/...

# ============================================
# Optional: Email Configuration
# ============================================

# SMTP configuration for email notifications (optional, future enhancement)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@project-swarm.com

# ============================================
# Development Tools
# ============================================

# Enable debug mode (shows verbose logs)
# DEBUG=true

# Skip authentication in development (USE WITH CAUTION!)
# SKIP_AUTH=false

# ============================================
# Notes & Tips
# ============================================

# 1. Required Variables:
#    - DATABASE_URL (PostgreSQL)
#    - SESSION_SECRET (generate unique secret)
#    - ENCRYPTION_KEY (generate unique key)
#    - ENCRYPTION_SALT (generate unique salt)
#    - At least one AI provider key (OPENAI_API_KEY, ANTHROPIC_API_KEY, or GEMINI_API_KEY)

# 2. Generating Secrets:
#    Run these commands to generate secure secrets:
#      openssl rand -hex 32   # For SESSION_SECRET and ENCRYPTION_KEY
#      openssl rand -hex 16   # For ENCRYPTION_SALT

# 3. Database Setup:
#    After configuring DATABASE_URL, run:
#      npm run db:push

# 4. Testing Configuration:
#    Test your configuration by running:
#      npm run dev

# 5. Production Deployment:
#    - Set NODE_ENV=production
#    - Use strong, unique secrets
#    - Configure database with connection pooling
#    - Set up HTTPS with valid SSL certificates
#    - Configure CORS for your domain
#    - Enable rate limiting
#    - Set up monitoring and logging

# ============================================
# Security Checklist
# ============================================

# ✅ Generated unique SESSION_SECRET
# ✅ Generated unique ENCRYPTION_KEY and ENCRYPTION_SALT
# ✅ Configured DATABASE_URL with strong password
# ✅ Added AI provider API keys
# ✅ Set NODE_ENV appropriately
# ✅ Never committed .env to version control
# ✅ Restricted file permissions (chmod 600 .env)
# ✅ Rotated secrets regularly (every 90 days recommended)
